# Fase 6 — Backend

Esta entrega implementa a base backend do módulo de Gestão de Combustíveis, Lubrificantes e Frota.

> O campo `fleets.marking_type` existente é usado diretamente: `H = Horímetro` e `K = Quilômetro`.

Não há Seeder nesta fase.
